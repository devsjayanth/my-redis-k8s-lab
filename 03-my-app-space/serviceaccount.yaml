# Dedicated identity for RedisInsight pods. Decouples pod permissions
# from the default service account. Referenced in the Deployment spec
# via serviceAccountName field.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: redisinsight-sa
  namespace: my-app-space
  labels:
    project: my-redis-k8s-lab